[project]
name = "pyo3_classes"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "maturin>=1.13.1",
]

[tool.maturin]
module-name = "pyo3_classes.native"
python-source = "python"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

