[project]
name = "pyo3tutorial"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "maturin>=1.13.1",
    "msgpack>=1.1.2",
]

[tool.maturin]
module-name = "simple_pyo3.native"
python-source = "python"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

